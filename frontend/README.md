# MUIS website (Next.js)

Public site, student login, and `/admin`.

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

Opens at http://localhost:3000  
API must be running at http://localhost:5000 (see `../backend`).
